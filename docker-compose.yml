version: '3'

services:
  db:
    image: postgres:11.2
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: local*db-pass+123
 
volumes:
  db-data:
    driver: local
